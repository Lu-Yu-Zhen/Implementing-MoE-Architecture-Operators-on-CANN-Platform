# ============================================================================
# 算子插件实现目录
# ============================================================================
# 本目录用于存放算子框架适配插件（如 TensorFlow/PyTorch 适配器）。
# 标准 CANN 算子工程中，框架插件由 CANN 的 msopgen 工具按需生成，
# 通常无需开发者手动编写。如需扩展框架支持，可在此目录添加对应实现。
# ============================================================================

# 默认无内容，框架适配由 CANN 工具链自动生成
